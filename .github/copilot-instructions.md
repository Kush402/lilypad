Follow AGENTS.md at the repo root. That is the protocol for this codebase's working memory (Cursor, Claude Code, Codex, Copilot).

Before non-trivial work, read `lilypad/Now.md`. If it is missing, the vault was not copied onto this machine — stop. The Obsidian vault is `lilypad/` (open that folder in Obsidian). `docs/` is the CI-enforced source of truth. Do not start a second defect ledger; proven findings go in `docs/kanban.md`.
