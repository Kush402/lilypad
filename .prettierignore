node_modules
dist
build
.turbo
apps/desktop/src-tauri/target
apps/mobile/ios/Pods
apps/mobile/android
pnpm-lock.yaml
# Drizzle-generated; drizzle-kit reformats these its own way on every
# `db:generate`, so hand-formatting them just creates repeated diff noise.
apps/backend/drizzle/meta
