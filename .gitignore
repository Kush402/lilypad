# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
out/
*.tsbuildinfo
.vite/
.cxx/

# Rust / Tauri
apps/desktop/src-tauri/target/
apps/desktop/src-tauri/gen/

# React Native
apps/mobile/ios/Pods/
# Where the versioned apps/mobile/.bundle/config sends `bundle install` — the
# CocoaPods gem tree the README's `bundle exec pod install` needs locally, and
# never something to commit.
apps/mobile/vendor/
apps/mobile/ios/build/
apps/mobile/ios/build-device/
apps/mobile/ios/build-release/
apps/mobile/android/.gradle/
apps/mobile/android/app/build/
apps/mobile/android/build/
apps/mobile/android/local.properties
apps/mobile/.expo/
*.keystore
*.jks
!debug.keystore

# Env & secrets
.env
.env.*
!.env.example
# Templates carry only placeholders and must be versioned; the `.env.*` rule
# above would otherwise swallow them along with the real secrets.
!infra/production/.env.production.example

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
*.log

# Turbo
.turbo/

# Coverage
coverage/

# Xcode per-user IDE state (window layout etc.) — never meaningful in git
**/xcuserdata/

# Derived code-intelligence indexes (rebuilt on demand, never committed)
.codegraph/
.code-review-graph/

# React Native pins the node path here per machine; the versioned
# .xcode.env already resolves it with $(command -v node).
apps/mobile/ios/.xcode.env.local
